<style>

/* navbar */
.hover-underline-animation {
        position: relative;
        display: inline-block;
    }

    .hover-underline-animation::after {
        content: '';
        position: absolute;
        width: 100%;
        height: 2px;
        background-color: #007bff;
        bottom: 0;
        left: 0;
        transform: scaleX(0);
        transform-origin: bottom right;
        transition: transform 0.25s ease-out;
    }

    .hover-underline-animation:hover::after {
        transform: scaleX(1);
        transform-origin: bottom left;
    }
    body {
        background: rgb(254, 254, 255);
    }

    /* General Card Styles */
   .card {
    background: #fffbfb;
    color: rgb(0, 0, 0);
    height: 100%;
    border: 2px solid #003366; /* Updated border color */
}


    .card-body {
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        height: 100%;
    }

    .card-img-top {
        height: 200px;
        object-fit: cover;
    }

    .card:hover {
        transform: translateY(-10px);
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    /* Button Styles */
    .btn,
    .truck-btn,
    .bus-btn,
    .vehicle-btn {
        background-color: #003366;
        color: white;
        font-weight: bold;
        border-radius: 6px;
    }

    .btn:hover,
    .truck-btn:hover,
    .bus-btn:hover,
    .vehicle-btn:hover {
        background-color: #002244;
        color: white;
    }

    /* Anchor tag */
    a {
        color: rgb(42, 235, 58);
        font-weight: 600;
    }

    /* Payment Form Styles */
    .payment-form {
        max-width: 500px;
        margin: auto;
    }

    .form-control {
        margin-bottom: 15px;
    }

    .payment-method {
        margin-top: 20px;
    }

    .btn-primary {
        width: 100%;
    }

    /* Order Card Styles */
    .order-header {
        background-color: #f8f9fa;
        padding: 1rem;
        border-bottom: 1px solid #dee2e6;
        border-radius: 0.375rem 0.375rem 0 0;
    }

    .order-card {
        border: 1px solid #dee2e6;
        border-radius: 0.375rem;
        overflow: hidden;
    }

    .order-item {
        transition: background-color 0.2s ease;
    }

    .order-item:hover {
        background-color: #f8f8f8;
    }

    .text-small {
        font-size: 0.875rem;
    }

    /* Generalized Vehicle Card Styles (Truck, Bus, Vehicle) */
    .truck-card,
    .bus-card,
    .vehicle-card {
        transition: all 0.3s ease;
        border: none;
        border-radius: 12px;
        overflow: hidden;
    }

    .truck-card:hover,
    .bus-card:hover,
    .vehicle-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    }

    /* Hero Overlay */
    .hero-overlay {
        background: rgba(0, 0, 0, 0.5);
    }
     .cart-item {
                background-color: #ffffff;
                border-radius: 10px;
                box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
                transition: all 0.3s ease;
            }

            .cart-item:hover {
                transform: translateY(-3px);
            }

            .cart-item img {
                border-radius: 10px;
                max-height: 120px;
                object-fit: contain;
                border: 1px solid #ddd;
                padding: 5px;
                background: #f8f8f8;
            }

            .summary-box {
                background-color: #fafafa;
                border: 1px solid #ddd;
                border-radius: 10px;
                padding: 25px;
            }

            .summary-box h5 {
                border-bottom: 1px solid #ccc;
                padding-bottom: 10px;
                margin-bottom: 20px;
            }

            .btn-remove {
                font-size: 0.9rem;
                padding: 8px 12px;
            }

            .empty-cart-img {
                max-width: 150px;
                margin-bottom: 20px;
            }
            
    .dashboard-card {
      transition: all 0.3s ease;
      border: none;
      border-radius: 12px;
    }

    .dashboard-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    }

    .dashboard-icon {
      color: #003366;
    }

    .custom-primary-btn {
      background-color: #003366;
      color: white;
      font-weight: bold;
      border-radius: 6px;
    }

    .custom-primary-btn:hover {
      background-color: #002244;
      color: white;
    }
    /* Fix product image size & aspect */
    .product-img {
                    object-fit: cover;
                    height: 200px;
                    width: 100%;
                }

                /* Wishlist button styling */
                .wishlist-toggle-form {
                    z-index: 10;
                }

                .wishlist-toggle-form button.btn-sm.rounded-circle {
                    width: 36px;
                    height: 36px;
                    padding: 0;
                }

                /* Button container spacing */
                .card-body .btn-group {
                    display: flex;
                    gap: 0.5rem;
                }
</style>