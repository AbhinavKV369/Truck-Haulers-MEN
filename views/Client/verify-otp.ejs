<!DOCTYPE html>
<html lang="en">

<head>
  <%- include("partials/head") %>
    <title>Verify OTP</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <%- include("partials/authScript") %>
</head>

<body class="d-flex justify-content-center align-items-center vh-100 bg-light">
  <div class="card shadow p-4" style="max-width: 400px; width: 100%;">
    <h3 class="text-center mb-4 fw-semibold">Verify OTP</h3>

    <form action="/verify-otp" method="POST"  novalidate>
      <input type="hidden" name="email" value="<%= email %>">

      <div class="mb-4">
        <label for="otp" class="form-label fw-medium">Enter OTP</label>
        <input type="text" id="otp" name="otp" class="form-control" placeholder="Enter your 6-digit OTP" maxlength="6"
          pattern="[0-9]{6}" required>
      </div>

      <button type="submit" class="btn btn-primary w-100">Verify OTP</button>
    </form>

    <div class="mt-3 text-center">
      <a href="/login" class="link-light text-decoration-none">← Back to Login</a>
    </div>
  </div>

  <%- include("partials/scripts") %>
    <%- include("partials/msgFlash") %>
</body>

</html>